# Settings — Notes

Designer scratchpad.

## Open questions

- **Required fields on a location** — do we require `pickupInstructions` when
  a location has any pickup-eligible day? Skipping the requirement for now;
  the website can render a sensible default.
- **Closed all day** — represented with a `Closed` toggle that disables the
  open/close time inputs. Is that the right model vs leaving times empty?
- **Default hours** — should "Add location" pre-fill 9–5 Mon-Fri? Currently
  blank. Pre-fill might save 90% of operators a click.
