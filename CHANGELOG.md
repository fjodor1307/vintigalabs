# Changelog

> Living handoff document for **repo-level** changes — design system, tooling, conventions, and workflows.
>
> **For prototype-specific changes**, see the `CHANGELOG.md` inside each prototype folder.
>
> **Convention:** Add new entries at the top. Each entry needs a date, who made the change, what changed, and why.

---

## Per-prototype changelogs

<!-- INDEX:START -->

| Prototype | Last updated | Changelog |
|-----------|--------------|-----------|
| Clubs | 2026-06-24 | [src/prototypes/clubs/CHANGELOG.md](src/prototypes/clubs/CHANGELOG.md) |
| Customers | 2026-07-10 | [src/prototypes/customers/CHANGELOG.md](src/prototypes/customers/CHANGELOG.md) |
| Onboarding | 2026-04-27 | [src/prototypes/onboarding/CHANGELOG.md](src/prototypes/onboarding/CHANGELOG.md) |
| Pos | 2026-06-25 | [src/prototypes/pos/CHANGELOG.md](src/prototypes/pos/CHANGELOG.md) |
| Pos Profiles | 2026-07-02 | [src/prototypes/pos-profiles/CHANGELOG.md](src/prototypes/pos-profiles/CHANGELOG.md) |
| Products | 2026-07-10 | [src/prototypes/products/CHANGELOG.md](src/prototypes/products/CHANGELOG.md) |
| Reservations | 2026-07-10 | [src/prototypes/reservations/CHANGELOG.md](src/prototypes/reservations/CHANGELOG.md) |
| Sales Chat | 2026-05-28 | [src/prototypes/sales-chat/CHANGELOG.md](src/prototypes/sales-chat/CHANGELOG.md) |
| Sales Chat Imessage | 2026-06-12 | [src/prototypes/sales-chat-imessage/CHANGELOG.md](src/prototypes/sales-chat-imessage/CHANGELOG.md) |
| Settings | 2026-07-08 | [src/prototypes/settings/CHANGELOG.md](src/prototypes/settings/CHANGELOG.md) |

_Auto-generated by `scripts/generate-index.mjs` — do not edit by hand._

<!-- INDEX:END -->

---

## 2026-07-15 — Fedja + Claude: Deck Builder — intro vs content slide types (Figma 5370:8871)

Made the Deck Builder slide-type aware, from the variations board. The **intro slide (page 1) is the distinctive one**; content slides stay simpler.

- **Intro slide** — carries a **Header position** control (across the **top** / vertical on the **left**), a **Media** mode (product **Composition** / plain **Image** / **None** = full-width headline), plus background + content fields. Marked with an "Intro" badge.
- **Content slides (2+)** — a simpler **Layout** picker: *Title + points, image right* (eyebrow + title + subtitle + a **points** editor + image), *Title + 3 cards* (a **cards** editor), *Text + media*, *Centered text*. No header-position options.
- The **live preview** renders each faithfully — the intro header top/left rail, the points-and-image "growth crisis" layout, and the 3 icon cards — across the backgrounds.
- **Backgrounds** — White · Black · Indigo · **Image** (photo only, no overlay) · **Image + 65% overlay** (darkened for white text). The plain Image option shows the photo untouched as the background.
- **Deck chrome** (Figma 5294:1345 / 5288:1091) — the real Vintiga **vertical logo lockup** (icon + "Vintiga Labs" wordmark, the provided `Logo.svg`) at the top of a **left rail**, with a **divider** and the **page number** at the bottom, on content slides and the left-header intro. The lockup inverts on dark backgrounds. Intro slides with a top header show the icon mark top-left.
- The generated **prompt** captures the intro's header + media and each content slide's layout, points and cards.

`PresentationsHub.tsx`.

## 2026-07-15 — Fedja + Claude: Deck Builder — full per-slide editor (Figma 5339:8004)

Rebuilt the Deck Builder's per-slide step (Step 2) from element-chips into the full editor, and renamed the screen title to **Deck Builder**:

- **Layout picker** — text · media / media · text / centered / full-media, each a mini-diagram glyph.
- **Background** — White · Black · Indigo · Image + 65% overlay (with an image switcher).
- **Content fields** — Caption · Headline · Description · Footnote · Image link.
- **Live branded 16:9 preview** that re-renders as you type — real kicker/title/lead typography, background theming, and the glass revenue card + avatars pill composed over the media.
- The generated prompt now carries each slide's layout, background and content.

`PresentationsHub.tsx`.

## 2026-07-10 — Fedja + Claude: Hub — prototypes open in a new tab; "Deck Builder"

- **Prototype clicks open in a new browser tab** — every prototype-opening affordance on the hub (card body, Prototype / Designs / Review links, screen thumbnails, and the arrow button) now opens in a new tab (`target="_blank"` / `window.open`), so the catalogue stays put behind the prototype. Design System / Brand / Presentations tool links stay same-tab.
- **"Page Builder" → "Deck Builder"** — renamed the Presentations → Builder hub card and wired it to `#/presentations/page-builder` (was "Coming soon"). The screen title is renamed on the Deck Builder PR.

`App.tsx`.

## 2026-07-06 — Fedja + Claude: Tone of voice refreshed from vintigalabs.com

The `vintiga-tov` skill was seeded in May and had drifted from the live site. Re-distilled from the current vintigalabs.com homepage:

- **Title Case for headings, buttons, CTAs and feature names** ("Book a Demo", "See Your Savings") — the site moved away from the old sentence-case-everywhere rule. Body copy and narrative subheads stay sentence case.
- **New "Signature constructions" section** — the recurring shapes on the site: *Turn X into Y*, verb-first outcome headlines (Know Every Guest), the em-dash qualifier (—without slowing down service), the two-beat punch (Spend Less. Sell More.), and the "No X. No Y." independence run.
- **Principle 1 is now "Outcome-first"** — every headline names what the operator gets; features are the supporting sentence.
- Voice samples replaced with current site lines; confidence beat updated to "Built in **2026** with zero technical debt". `references/word-swaps.md` capitalisation section updated to match, and the CLAUDE.md pointer notes the refresh date.

---

## 2026-07-05 — Fedja + Claude: Presentations — product mockup photography in the Overview deck

The Vintiga Overview deck now shows the product, not just the scenery — using the latest **in-context** mockup shots from Brand → Imagery (reference frames/collages stay out of the deck).

- **The Wedge (slide 5)** — swapped the pergola bar for `mockups/iphone-guest-profile-wine.jpg`: the Vintiga guest profile on iPhone at the tasting table.
- **The Solution (slide 7)** — swapped the lifestyle terrace for `mockups/dashboard-office-portrait.jpg`: the Vintiga dashboard at work in the office.
- **Early Validation & Differentiators (slide 9)** — added an image column (lg+) with `mockups/dashboard-screen-closeup.jpg`, a close-up of the Vintiga dashboard on screen. Hidden below `lg` so mobile stays single-column text.

---

## 2026-07-03 — Fedja + Claude: Presentations — Vintiga Overview deck

First entry in the **Presentations** hub tab (previously a placeholder): a full-screen, branded slide deck built from the *Vintiga Overview (July 2026)* PPTX.

- **`src/presentations/VintigaOverviewScreen.tsx`** — 17 slides at `#/presentations/vintiga-overview`, using Vintiga tokens + Inter type and brand photography from `public/brand/imagery/`. Dark title / divider / closing / funding slides, light content slides, and an indigo stat slide — one dominant colour (indigo), a visual on every slide.
- **Snappy transitions** — each slide fade-rises in (`deckIn` 360ms) with staggered `fadeUp` content; a thin indigo progress bar tracks position.
- **Navigation** — keyboard (←/→/Space/Home/End, Esc to exit) + on-screen prev · counter · next; exit returns to the hub on the Presentations tab.
- **Hub** — `PresentationsSectionCards` renders the deck as a cover-image card on the **Presentations** tab (App.tsx); route registered in `webScreens`.

Content mirrors the deck: problem → why now → tasting-room stats → the wedge → outcomes → solution → cost savings → validation → market → team → GTM → financials → funding.


Restructured **Brand → Imagery** into one consistent three-tier shape — **Collection › Subject › Set** — so any future folder drops in the same way.

- **Collections** (was Compositions / Character Sheets / Locations / Mockups): **Personas**, **Locations**, **Mockups**.
- **Subjects** inside each: Personas → **Sarah**, **Mika**; Locations → **Maison Soleil** (outdoor estate), **La Cave** (indoor); Mockups → **iPhone**, **iPad**.
- **Sets** every subject holds: **Reference** (clean / establishing) + **In context** (lived-in scenes), rendered as gallery sections with a per-section and per-subject **Download zip** and a lightbox. Empty sets show a "No images yet" placeholder.
- **Tags** replace CRM/POS: cards now say what's inside — **Reference / In context** — or a location's **Outdoor / Indoor** type (new optional `kind`). Dropped the now-unused `surfaces` field and `SurfaceBadge`.
- **New photography** (from Figma `Sheet-1`): six Maison Soleil estate shots (terrace, vineyard rows, pergola bar, garden path, veranda lounge, barrels) + the La Cave interior mood board.

## 2026-07-01 — Fedja + Claude: Brand → Imagery gallery

Built a marketing image library at **Brand → Imagery** (`#/brand/imagery`) so the marketing team can browse and download brand photography (Figma 6425-1288 / 6426-1491 / 6426-1698).

- **Collections index** — a list of image collections (Compositions, Character Sheets, Locations, iPhone & iPad Mockups) with badges, description, tags, **View gallery** + **Download zip**.
- **Gallery** — **grid** (default) / **list** toggle, plus a **Download zip** of the whole collection (zips client-side via `jszip`, dynamically imported so it stays out of the main bundle).
- **Lightbox carousel** — click any image for a full-size view with **prev · download · next** and keyboard arrows / Esc.
- **Downloads** — per-image (lightbox + list rows) and per-collection zip.
- Images are plain files under `public/brand/imagery/<collection>/`, listed in `src/brand/imageryData.ts`; missing files render as neutral placeholder tiles. See `public/brand/imagery/README.md` to add photos. Seeded the first four brand photos (Compositions ×2, Character Sheets, Locations).
- **Hub chrome:** the Imagery pages now render inside the real **hub navbar** (extracted to `src/hub/HubNavbar.tsx` and shared with the home page) — logo · tabs · search · dark toggle · Latest updates · download. Breadcrumbs: **Brand › Imagery** on the index, **Imagery › <collection>** in a gallery.
- Collection thumbnails sized so the Imagery cards match the **prototype card height** (~266px), rather than forcing the card taller.

## 2026-06-30 — Fedja + Claude: Latest updates — drop the Today range

Removed the **Today** range from the Latest updates modal — it now offers **This Week / Last Week** only, defaulting to **This Week**.

## 2026-06-30 — Fedja + Claude: Brand tab

Added a **Brand** tab to the hub (between POS and Design System) — the identity layer, kept distinct from the Design System (the build kit). It shows **section cards**:

- **Logo / Colour / Typography** deep-link into the Design System (`?p=logo` / `colors` / `typography`) — no duplication.
- **Tone of voice** opens a new in-app page (`#/brand/tone-of-voice`) built from the `vintiga-tov` rules — principles, hard rules, length budgets, voice samples. (Tone of voice had no home before.)
- **Imagery** is a "Coming soon" placeholder.

## 2026-06-30 — Fedja + Claude: Design System tab → section cards

The **Design System** tab in the hub now shows three **section cards** — **Foundation**, **Assets**, **Components** — instead of jumping straight into the DS. Each card **deep-links into the design system at that section** (`#/web/design-system?p=<page>` → Foundation/colors, Assets/logo, Components/buttons). `DesignSystemScreen` reads the initial page from the URL. The All tab's Design System row reuses the same three cards.

## 2026-06-29 — Fedja + Claude: Hub chrome on the design system + cleaner updates feed

Polished the hub to use the design-system components consistently:

- **Logo** bumped to **36×36px**.
- **Navbar controls use DS components at a uniform 40px height** — search is now the DS `TextField`, and the dark toggle, Latest-updates, and download are all 40px (download/Latest-updates are DS `IconButton`s). Sub-header sort + grid/list switch use the DS `Button` / `IconButton`.
- **Card footer links:** **Prototype · Designs · Review**, with the **arrow** (DS `IconButton`, solid on the featured card, outline elsewhere) opening the **live prototype**. Center-aligned in one line.
- **DS `TextField` gained dark-mode support** (only active under the hub's `.dark`, so prototypes are unaffected).
- **Latest updates feed is now PM/CEO-friendly:** only user-facing changes (features / fixes), capitalised, de-duplicated, and capped at 6 per area with a "+N more". Modal buttons use DS `Button`.

## 2026-06-29 — Fedja + Claude: "Latest updates" modal (end-of-day summary)

Added a **Latest updates** modal to the hub, opened by a clock icon in the navbar — a quick summary of what shipped, handy for end-of-day reports.

- **Time ranges:** Today / This Week / Last Week (calendar weeks).
- **Grouped by area:** each prototype (e.g. `Clubs {CRM}`), plus `Builder {Hub}` and `Design System {DS}`, with a bulleted list of changes and a **Latest PR** link.
- **Area filter** dropdown + **Open Github** button (deep-links to the repo's PRs).
- Data is generated from git history at build time — new `scripts/generate-updates.mjs` → `src/generated/updates.json`, wired into `predev`/`prebuild` (and `npm run updates`).

## 2026-06-29 — Fedja + Claude: Hub featured card + sub-header (Figma 6413-1139 / 6414-1712 / 6414-18106)

Brought the hub catalog up to the latest Figma for both grid and list:

- **Featured "Latest" card.** The first prototype is now highlighted — indigo-tinted background, a "Latest" label beside the badge, and a filled indigo arrow. In **grid view** it becomes a large card spanning two columns with a row of live screen thumbnails inside; the two next prototypes stack beside it, and the rest fall into a uniform grid. In **list view** the first row gets the same indigo treatment.
- **Catalog sub-header.** Added a header row above the catalog: the active category title on the left, a **sort menu** + the **grid/list switch** on the right. The view switch moved out of the navbar to live here. The sort menu offers **Last updated** (real recency from git history, default), **Name (A–Z)**, and **Most screens**; the featured card is whichever prototype sorts first. Selection persists in `localStorage`.
- **Arrow affordance** is now a square button on every card — filled indigo on the featured card, outlined elsewhere (20px icon, per design).
- **Toggle icons** use lucide **`grid-2x2`** and **`layout-list`**.
- Sort preference persists in `localStorage`.
- **Design tune-up** (matching Figma 6414-2689): featured card border → `indigo-200`, gaps 20/24/4px, medium-weight title, indigo `#tag` pills, thumbnails 244px tall. All cards use **24px padding** (`p-vintiga-lg`).
- **Dark mode fix:** the featured card's indigo tint stayed light in dark mode (washing out the text). Enabled class-based `dark:` utilities (`@custom-variant dark`) and gave the featured card (grid + list) the same dark surface/border as the other cards in dark mode; its arrow stays indigo.
- **Featured tags** now use the same neutral pill style as every other card (dropped the indigo variant), for consistency.

## 2026-06-26 — Fedja + Claude: Hub redesign — cards + dark mode (Figma 6401-2361 / 6407-936)

Updated the prototype hub to the latest Figma, light and dark:

- **List view + grid/list switch** (Figma 6413-1139 / 6414-1712). A new toggle button in the navbar flips the catalog between the card **grid** and a **list** — each list row shows the card meta on the left plus a horizontal strip of **live, click-through screen thumbnails** (real iframes of the prototype's screens; clicking one launches the prototype at that screen, capped at 8 per row). Preference persists in `localStorage`.
- **Category badge above the title** (top-left).
- **Badge colours + style:** CRM → **lime/green** (`lime-100` / `green-700`), POS → **indigo** (`indigo-100` / `indigo-700`); dropped the status dot; rounded-2xl pill. **Badges keep their colour in dark mode** (per the dark Figma).
- **"Designs" link drops the screen count** — just "Prototype · Designs" + arrow.
- **Dark mode reworked to the neutral scale + elevation:** page `#0a0a0a` (neutral-950) with cards raised on `#171717` (neutral-900) and `#262626` (neutral-800) borders; muted text/links neutral-300. Toggle matches the Figma (light = dark track + sun, knob right; dark = neutral-800 track + moon, knob left).

## 2026-06-25 — Fedja + Claude: Prototype hub — fixed frosted navbar

Rebuilt the hub home page around a **sticky, frosted-glass top navbar** (mirrors the Design System header: `sticky top-0 + bg-surface/75 + backdrop-blur-md + border-b`). The navbar holds the **logo · category tabs (All · CRM · POS · Design System · Presentations) · search · download**; the big "Vintiga Prototypes" title and the separate filter bar are gone, and the card grid scrolls underneath.

- **Card footer trimmed:** dropped the "Review →" text + contributor avatars — the review link is now a bare **→ arrow icon**. (This is the change that was lost when #117 merged before it landed.)
- Removed the dead `AvatarStack` + contributor-data plumbing and the now-unused `FilterBar` usage from `App.tsx`.
- **Dark mode switch** in the navbar — a **neutral-only** dark theme using Tailwind's **neutral** scale (true gray, no blue tint): surfaces/text/borders + brand accents all map to neutral, so CRM/POS badges and links go grey. Scoped to the hub via a `.dark` class on the hub container, so **prototype screens stay light**. Preference persists in `localStorage`. New `SunIcon`/`MoonIcon` aliases.

## 2026-06-25 — Fedja + Claude: Home-page card links + category tabs

Two home-page tweaks:

**Prototype / Designs links in the card.** The floating Prototype/Design pill overlapped each prototype's own UI, so it's gone. The choice now lives **on the card** — "Open flow (N screens)" is replaced by two links, **Prototype** (opens the live prototype) and **Designs (N screens)** (opens the design overview).
- The **live prototype renders with no builder chrome** — pristine, like the real app.
- The **Designs (overview)** page keeps a minimal **"Prototypes"** back link (top-left).
- Removed `FrameToolbar`.

**Category tabs.** The filter now uses the DS `SegmentedControl` (white-pill style) with **All · CRM · POS · Design System · Presentations** (Presentations is a placeholder category for now). The **Author filter is removed**. `FilterBar` no longer takes author props.

## 2026-06-25 — Fedja + Claude: POS components in the Design System (Mobile Patterns)

Added the POS chrome components from Figma [06. POS App](https://www.figma.com/design/V8VUTuLgW2cCCUnJh2xJkN/06.-POS-App?node-id=2955-6116) to the **Design System → Mobile Patterns** group (alongside Screen Header / Screen Footer) — as reusable DS components, not a prototype:

- **`PosTabBar`** (`@ds/shared/PosTabBar`, Figma `2955:6081`) — floating glass tab bar; active tab floats in a white pill with the indigo accent.
- **`PosNavbar`** (`@ds/shared/PosNavbar`, Figma `2955:6082`) — product search navbar + list-view toggle.
- **`PosCartButton`** (`@ds/shared/PosCartButton`, Figma `2955:6123`) — floating cart button with a red live-count badge.
- **`PosProductCard`** (`@ds/shared/PosProductCard`, Figma `2955:6142`) — square product tile with a frosted name · price · volume panel; tap to add to cart.

Each has a showcase under `ComponentsSection` and a nav entry under Mobile Patterns. Also added a `List → ListIcon` alias to the shared icon set. These power the new **POS / Products** prototype (`src/prototypes/pos/` — see its changelog).

## 2026-06-25 — Fedja + Claude: CRM / POS / Design System categories + lighter prototype chrome

Reworked the prototype-builder home page and the in-prototype chrome:

- **Categories replace status.** The home filter is now **All · CRM · POS · Design System** (single-select) instead of All / In progress / Approved. A prototype's category is derived from its `frame`: **web → CRM** (dashboard designs), **mobile → POS** (mobile designs). Per-card badges now read **CRM** (indigo) or **POS** (teal). Selecting **Design System** shows only the Design System tool.
- **No more dark top bar.** The full-width `bg-slate-800` bar on prototype views is gone. The **Prototype / Design** toggle + back link now float as a compact pill at the bottom-centre ("inside the card"), so they never overlap a prototype's own header.
- **Flow view removed entirely** — the third tab and its `FlowGrid` are deleted; only Prototype + Design remain.
- `FilterBar`'s segmented control is now generic (`segments` / `activeSegment` / `onSelectSegment`) rather than status-specific.

## 2026-05-27 — Removed the `experience-timeslots-matrix` prototype

Deleted `src/prototypes/experience-timeslots-matrix/`. The Reservation Time Slots grid + Blackout Dates table that lived there have been folded into the **Products → Schedule** tab, and the May 27 review concluded that's where the work should live going forward (one editor, not a parallel sandbox). Everything we built in the matrix prototype is now in `src/prototypes/products/TimeSlotsScreen.tsx`.

## 2026-05-20 — Removed the standalone `experience-timeslots` prototype

Deleted `src/prototypes/experience-timeslots/`. It was an exploratory sandbox for the experience scheduling UX; that work is now consolidated into the **Products → Schedule tab** (booking settings, generate slots, matrix-style blackout dates). The `experience-timeslots-matrix` exploration stays for reference, and the shared `_shared/ExperienceShell` keeps the renamed "Schedule" tab label.

Why: no need to maintain a parallel sandbox now that the schedule design lives in the real product editor.

---

## 2026-05-11 — Icon palette: chat-flow additions (`CheckCheck`, `Paperclip`, `Smile`, `Mic`, `Layers`)

Added five lucide icons to the DS palette to support messaging-style UIs: `CheckCheckIcon` (read receipts), `PaperclipIcon`, `SmileIcon`, `MicIcon`, `LayersIcon` (template indicator). Re-exported from `@ds/icons/Icons` so usage stays consistent across prototypes.

Why: the new `sales-chat` prototype needs all five for a WhatsApp Business–style inbox; bundling them in the central re-export keeps imports uniform.

---

## 2026-05-11 — `AppSidebar` — Sales Chat nav item now routes to `#/web/sales-chat`

The Sales Chat entry in the top nav was a label without a destination. Now points at the new prototype (`src/prototypes/sales-chat/`).

---

## 2026-05-07 — Responsive shell — sidebar collapses to drawer below `md`

The dashboard shell didn't honour the design rule (DESIGN-RULES.md: "Sidebar hidden below `md` (768px), mobile overlay on toggle"). Every layout was hard-coding `Navbar device="desktop"` and the inline sidebar stayed visible at every viewport, squeezing content below tablet width.

Now lives in the DS so every prototype gets responsive chrome for free:

- New hook `@ds/shared/useResponsiveSidebar` — single source of truth for `collapsed` (md+ icon-only) + `mobileOpen` (sub-md drawer). `onMenuToggle` does the right thing per viewport via `matchMedia`. Pair with `<Navbar device="responsive" onMenuToggle={onMenuToggle} />` and `<AppSidebar collapsed mobileOpen onMobileClose />`.
- `AppSidebar` rebuilt as two render paths: desktop wrapper `hidden md:flex` (inline, takes layout space) + mobile fixed overlay `md:hidden` with backdrop dismiss. Both share an internal `AppSidebarInner` so nav stays in one place. `onItemClick` fires on every nav item in the mobile drawer so tapping a destination closes the drawer too.
- All seven layouts swept to use the hook + `device="responsive"`:
  - `customers/CustomerViewLayout` + `customers/CustomersScreen`
  - `clubs/ClubsLayout` + `clubs/ClubViewLayout` + `clubs/ClubEditorLayout`
  - `products/Shell` + `products/ProductLayout`
  - `onboarding/DashboardScreen` (uses bare `Sidebar` so the same wrapper pattern is inlined; nav extracted into a local `DashboardSidebar` component)

Verified end-to-end in preview at 375×812 (mobile) and 1280×800 (desktop): inline sidebar disappears below md, hamburger appears, drawer slides in, backdrop click + nav-item tap both dismiss, viewport-resize back to md+ restores the inline sidebar.

## 2026-05-07 — KpiCard `sm` rebuilt to match Figma "KPI-small" (5682:137241)

`KpiCard` size `sm` swapped from the old "icon-top + label/value bottom" 2-row layout to the Figma `KPI-small` component: a 64-px tall single-row card — icon (32-px indigo circle) on the left, label flex-grows, value pinned right, optional chevron on the trailing edge.

- The `href` prop now also drives the **chevron**: card with `href` → renders as `<a>`, gets a slate-50 hover tint and a trailing `ChevronRightIcon` (matches Figma's `Button=True` variant). Without `href` → no chevron, no hover (Figma `Button=False`).
- Hover-state colour pair changed from indigo-tint (`indigo-50/30` + `indigo-300` border) to the more neutral slate pair the Figma uses (`slate-50` fill + `slate-300` border).
- Style guide showcase at `Components → Data Display → KPI Card → ⑤ Compact` now demonstrates both variants — the default (4 cards, no chevron) and the clickable (2 cards with chevron).

Consumers that change visual:
- `customers/CustomerOverviewScreen` — Customer Insights swapped from a 3-col grid of big `md` KPIs to a 2-col grid of compact `sm` KPIs (Account Balance + Loyalty Points carry the chevron and route to the Billing transactions sub-pages). Matches Figma `5682:136146`.
- `clubs/ClubViewReleasesScreen` — same 6 KPIs, same 3-col grid, just rendered horizontally now. Verified in preview, still reads cleanly.

## 2026-05-07 — PageTemplate: Tailwind UI 2:1 grid for main + rail

`PageTemplate` switched from "flex with a fixed 360-px aside" to a CSS-grid two-column shell:

```
grid grid-cols-1 gap-vintiga-lg lg:grid-cols-3
  ├─ <main>  lg:col-span-2  (2/3 width)
  └─ <aside> lg:col-span-1  (1/3 width)
```

Why: align with the Tailwind UI page template the team is standardising on, so every "main + rail" page (Customer Overview, Product detail, New Club editor, etc.) shares one shape regardless of viewport width. The previous fixed 360-px aside drifted in proportion as the viewport grew; the grid keeps the 2:1 ratio at every `lg+` breakpoint.

Side-effects:

- The 84-px top inset on the rail is gone — the first rail card now aligns with the breadcrumb at the top of the main column (matches the Customer Overview Figma, where Notes sits at breadcrumb level).
- Outer padding moves up one wrapper: `p-vintiga-xl` is on the grid container, not on `<main>`. Same total padding, just a different element owns it.
- Below `lg`, the rail still stacks under the main column (now via `grid-cols-1`, no longer a flex-direction switch).
- No API change. All three consuming prototypes (Customers, Products, Clubs) pick this up automatically.

## 2026-05-05 — Page Template restructure + responsive rail

- DS nav: pulled `Page Template` out of `Data Display`. New `Templates` sub-group under Components — first entry `Products` (id `ds-template-products`). Leaves room for `Clubs` and other named templates.
- `PageTemplate` is now responsive — rail used to be `hidden lg:flex` and disappeared below 1024 px. Now it stacks below the main column on smaller widths (full-width, `px-vintiga-xl pb-vintiga-xl`) and switches back to a 360-px right column at `lg+`.
- Products prototype refactored to follow the canonical pattern: plain string title (uses `typo-title-screen` from `PageTemplate`), `Save` + kebab `IconButton` actions (Duplicate / Archive), and `RailSection`s passed directly to `PageTemplate`'s `rail` slot. Drops the custom thumbnail/tags header and the `<RightRail>` wrapper that was double-padding the aside.
- Clubs prototype rail aligned the same way — `ClubDetailsRail` no longer wraps in `<RightRail>`. Both editors now share identical rail spacing.

## 2026-05-04 — KpiCard: three variants from Figma (132:6062)

Brought `@ds/shared/KpiCard` in line with the Figma source of truth. The component now covers all three documented variants on a single API:

1. **Plain** — `label` + `value` + `icon`
2. **Goal / progress** — adds `goalLabel` + `progressPercent` (renders the progress strip below the value)
3. **Status pill** — adds `status?: ReactNode` slot (e.g. `<Tag variant="outline">Awaiting first sale</Tag>`) inline with the value. Combine with goal/progress freely — the "AOV $0 / Awaiting first order / No goal set 0%" empty state from Figma renders correctly.

Other tweaks:
- Icon container resized 40 → **36 px** (matches Figma) and forces 20 px child SVGs via `[&>svg]:w-5 [&>svg]:h-5`. Existing usages don't need to pass `w-* h-*` on the icon.
- Progress section now renders whenever `goalLabel` is set, even without an explicit `progressPercent` (defaults to 0% — supports the "No goal set" state).

Style guide entry refreshed at `#/web/design-system → Data Display → KPI Card` with a row per variant.

## 2026-05-05 — `PageTemplate` DS layout primitive (Figma 5640:28214)

New shared component `@ds/shared/PageTemplate` — the canonical editor / detail page layout. Composes existing DS pieces (Breadcrumb, Button, IconButton, SegmentedControl, SectionCard, RailSection) into the standard shape:

```
┌──────────────────────────────────────────┬─────────────┐
│ Home > Section > Page                    │             │
│                                          │  Title      │
│ Title                  [Save] [⋯]        │  ┌────────┐ │
│                                          │  │ rail   │ │
│ [Tabs · · · · · ]                        │  │ card   │ │
│                                          │  └────────┘ │
│ ┌────────────────────────────────────┐   │             │
│ │ SectionCard …                       │   │             │
│ └────────────────────────────────────┘   │             │
└──────────────────────────────────────────┴─────────────┘
```

- Main column: `p-vintiga-xl` (32 px) with 32-px gap between header rows (breadcrumb / title / tabs / body).
- Body sections gap: configurable, default `lg` (24 px).
- Right rail: `w-[360px]` · `pt-[84px] pr-vintiga-xl pb-vintiga-xl` so the first rail card lines up with the page title.
- Mobile (<lg): rail is hidden — pages can drop the prop or render the rail content as a section in the body.

Style guide: new "Page Template" entry under Data Display with two examples — full layout (with tabs + rail) and a single-column form (no tabs, no rail). Vocabulary updated.

## 2026-05-05 — `Media` DS component (promoted from products) + Checkbox handler fixes

- **New shared component** `@ds/shared/Media` — image library section (SectionCard + 4-col grid + Primary badge + indigo dashed dropzone tile + empty state). Supports `variant="bare"` for embedding in an existing card. Promoted from `products/MediaSection.tsx`.
- **Migrated** `products/MediaSection.tsx` → thin shim over `<Media>`. Added `productActions.addImages(files)` to match the DS callback signature.
- **Wired into Clubs** — `ClubOverviewScreen` now uses `<Media>` (replaces the inline "Drop or upload" placeholder). `clubStore` gained `images` state + `addImage / addImages / removeImage` actions.
- **Checkbox bug fix** — DS `Checkbox` calls `onChange(nextBoolean)`, not `onChange(event)`. Fixed three places in clubs that were treating the arg as an event and reading `(e.target as HTMLInputElement).checked` (which silently produced `undefined`). Affected: `ClubOverviewScreen` (auto-renew + accept-terms) and `ClubEditorEmailsScreen` (use-global-template).
- Style guide: new "Media" entry under Data Display with both populated + empty states. Vocabulary updated.

## 2026-05-04 — `Select` DS component (promoted from products)

New shared component `@ds/shared/Select` — native styled `<select>` matching Vintiga `TextField` chrome (h-10, slate-200 border, rounded-md, ChevronDown affordance, indigo focus ring). Accepts either a string array, a `{value, label}[]` array, or raw `<option>` children. Promoted from the inline `Select` that previously lived in `products/ProductLayout.tsx`.

Used by the new Club editor; the products inline copy will be migrated next.

## 2026-05-04 — `ClubCard` DS component (replaces `MediaListItem`)

Renamed and reshaped the bordered media-rich row pattern to match Figma 5636:24752 (`club-card` with `State=Default` + `State=Hover`):

- New shared component `@ds/shared/ClubCard` — each row owns its border + radius and lights up on hover (slate-50 bg, slate-400 border). Stack rows in a plain `flex-col gap-vintiga-md` container — no divided wrapper needed.
- Removed `@ds/shared/MediaListItem` — superseded by `ClubCard`. The only consumer (Clubs prototype) was migrated; no external usage to break.

Updates: style guide entry refreshed at `Components → Data Display → Club Card`, vocabulary list updated, Clubs prototype switched over.

## 2026-05-04 — Clubs prototype scaffolded

New `clubs` prototype at `#/web/clubs` (3 routes — Clubs / Memberships / Club Emails). Built from Figma node 5079:44185.

## 2026-04-23 — Template initialised

Starting point for Vintiga's prototype repo. Stripped all brand-specific content from the template so the new team starts with a blank canvas.

**Kept:**
- React 19 + Vite + TypeScript + Tailwind 4 + Base UI scaffold
- `src/design-system/` — tokens, icons, generic shared components (Button, TextField, Checkbox, Radio, Switch, AlertSoft, Card, BottomSheet, ScreenHeader, ScreenFooter, StatusBar, PhoneFrame, etc.)
- Prototype registry — `src/prototypes/_registry.ts` auto-discovers `prototype.config.ts` in each subfolder
- Scripts — `new-prototype`, `clone-prototype`, `generate-index`, `generate-contributors`, `install-hooks`
- Templates — `_templates/CONTEXT.md`, `JOURNEY.md`, `NOTES.md`, `CHANGELOG.md`
- Claude skills — `new-prototype`, `clone-prototype`, `publish-prototype`, plus a `vintiga-tov` stub to fill in
- Style guide at `#/web/style-guide`

**Stripped:**
- All prior-tenant prototypes (`src/prototypes/{onboarding,subscription,...}`)
- Prior-tenant context (`_context/programme.md`, `personas/sophie-carter.md`, `brand/essentials.md`, `research/`, `competitive/`)
- Prior-tenant brand assets — bespoke font, logos, photography, illustration pack
- Prior-tenant components — AccountCard, AccountInfo, PersonCard, PasswordChecklist, MilestoneImage, CustomerHeader, and the legacy card component
- Dependencies only used by deleted components — `banks-logo`, `country-flag-icons`, `react-pay-icons`, `wrangler`

**Genericised:**
- `src/design-system/tokens.css` — palette reduced to neutral greyscale placeholders. Replace primary/accent colours and fonts with Vintiga brand values when defined.
- `DESIGN-RULES.md` — same rules, brand-specific hex values replaced with placeholders.
- All legacy `*-` class/token names renamed to `vintiga-*`.

**TODO for the Vintiga team:**
- Fill in `_context/programme.md`, `_context/personas/`, `_context/brand/essentials.md`
- Set brand values in `src/design-system/tokens.css` (primary colour, fonts)
- Expand `.claude/skills/vintiga-tov/SKILL.md` with real tone-of-voice rules
- Scaffold the first prototype: `npm run new-prototype <slug>`

## 2026-04-23 — Scaffolded `products` prototype

Created `src/prototypes/products/` via `npm run new-prototype`.

## 2026-05-04 — Scaffolded `clubs` prototype

Created `src/prototypes/clubs/` via `npm run new-prototype`.

## 2026-05-16 — Scaffolded `experience-timeslots` prototype

Created `src/prototypes/experience-timeslots/` via `npm run new-prototype`.

## 2026-05-16 — Scaffolded `experience-timeslots-matrix` prototype

Created `src/prototypes/experience-timeslots-matrix/` via `npm run new-prototype`.

## 2026-05-25 — Scaffolded `interactive-tasting-method` prototype

Created `src/prototypes/interactive-tasting-method/` via `npm run new-prototype`.

## 2026-07-02 — Scaffolded `pos-profiles` prototype

Created `src/prototypes/pos-profiles/` via `npm run new-prototype`.
